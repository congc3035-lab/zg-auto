# 开发契约

本文件是硬约束，不是建议。违反其中任何一条的改动，一律退回重做。

方法论基线：[Superpowers](https://github.com/obra/superpowers)。
本文件是它在本项目约束下的落地版本 —— 保留其两条铁律，替换其无法在本项目执行的环节。

## 零、两条铁律

```
没有先失败的测试，不写任何 production 代码
没有刚跑过的验证证据，不说「完成」
```

**第一条**：先写测试 → 亲眼看它失败 → 写最小实现 → 亲眼看它通过。
若代码写在测试之前，删掉重来。不留作「参考」，不「改造复用」，删就是删。

**第二条**：任何「通过了 / 修好了 / 完成了」的说法，都必须附带**本次刚跑出来的**证据。
「应该没问题」「我很有信心」不是证据。没跑就说通过，等于说谎。

这两条对所有参与者生效，包括 AI 助手。

## 一、六步流程

每个开发任务必须依次走完，**两道关卡由仓库所有者放行**：

### 1. 更新需求规格
在 `docs/01-需求规格.md` 写清做什么、为什么、验收标准。只提交文档，不动代码。

### 2. 编写实施计划
产出 `docs/plans/YYYY-MM-DD-<任务名>.md`，把工作拆成 **2–5 分钟一步**的任务。

计划要**假设执行者对本项目零上下文、判断力可疑、且讨厌写测试**。因此每个任务必须包含：

- **确切文件路径**（创建哪个、修改哪个的哪几行、测试文件在哪）
- **接口契约**（消费上游的什么签名、向下游提供什么签名）
- **完整可粘贴的代码**，不是描述
- **具体验证命令与预期输出**

**禁止出现的占位（属于计划缺陷，不是省略）：**

- `TBD`、`TODO`、「稍后实现」、「补充细节」
- 「加上适当的错误处理」「做好校验」「处理边界情况」
- 「为上述内容写测试」而不给出测试代码
- 「与任务 N 类似」—— 重复写一遍，执行者可能乱序阅读
- 引用了任何任务都没定义过的类型或函数

写完计划后自查三项：**规格覆盖**（每条需求能否指到一个任务）、**占位扫描**、**类型一致性**（任务 3 叫 `clearLayers()`、任务 7 叫 `clearFullLayers()` 就是 bug）。

> **关卡一 · 计划确认**：所有者放行后，才能修改 `src/`。

### 3. 逐任务 TDD 执行
对每个任务严格走红绿循环：

1. **RED** —— 写一个最小的失败测试，只测一个行为，名字说清行为
2. **验证 RED** —— 跑测试，**亲眼确认它失败**，且失败原因是「功能缺失」而非拼写错误
3. **GREEN** —— 写刚好能让它通过的最少代码，不多写一行（YAGNI）
4. **验证 GREEN** —— 跑测试，确认通过，且其余测试仍全绿、输出干净无警告
5. **REFACTOR** —— 只在绿灯下清理重复、改名、抽helper，不加行为
6. **提交** —— 一个任务一次提交

**测试立刻就通过 = 你在测已有行为，测试写错了。**

### 4. 验证
两类证据缺一不可：

- **自动化测试** —— GitHub Actions 全绿，附运行链接
- **Dry-run 日志** —— 开启 dry-run 跑一轮，贴出「本应请求的 URL 序列」

> **关卡二 · 执行确认**：所有者核对两类证据后放行，才能关掉 dry-run 实跑。

### 5. 实跑并记录
关掉 dry-run 执行一轮，对照预期逐条核验，异常当场记录。

### 6. 更新变更日志
在 `docs/03-变更日志.md` 追加一条。只追加，不改写历史。

## 二、遇到问题就停

**立即停止执行并询问所有者**的情形：

- 撞上阻塞（依赖缺失、测试反复失败、指令含义不明）
- 计划有关键缺口，无法开始
- 验证反复不通过

**不要猜，不要硬闯。** 若实现过程中发现必须改动计划声明之外的位置，**退回第 2 步重新报批**，不得就地扩大范围。

## 三、测试基线

**本项目不在本地磁盘保存代码**，因此测试跑在 GitHub Actions 上。

- 工作流：`.github/workflows/test.yml`
- 命令：`node --test tests/`（Node 20 自带 test runner，零依赖，不引入构建步骤）
- 触发：每次 push 与 pull request
- 反馈约 30–60 秒，比本地慢，换来的是真实的红绿循环而不是「我觉得没问题」

**开工前必须确认测试基线是绿的。** 基线不绿就开始开发，等于在流沙上盖房子。

### 能测什么、不能测什么

诚实划界，不假装覆盖率。

| 层 | 可测性 | 手段 |
|---|---|---|
| L1 Core · HTML 解析 | **可单测** | 纯函数，喂 `fixtures/` 里的真实页面，断言输出 |
| L2 Gateway · token 与登录检测 | **可单测** | 注入假的 http，断言请求序列与异常分支 |
| L3 Domain | 部分 | 时间窗口判定是纯函数，可测；编队切换依赖真实服务端，不可测 |
| L4 Tasks | **不可单测** | 依赖真实页面与服务端状态 |
| L5 Runtime | **不可单测** | 依赖浏览器定时器与 DOM |

L3 以上靠 **dry-run** 覆盖：`gw.act` 只记录「本应请求的 URL」而不真发，
完整走一遍全流程而不改动账号任何状态。这是这部分唯一的验证手段。

### 解析函数必须是纯函数

所有 HTML 解析写成 `(html) => value`：不碰网络、不碰 `localStorage`、不碰 DOM 全局。
只有这样才能脱离浏览器测试。游戏改版时，对 `fixtures/` 重跑解析函数即可定位是哪个页面变了。

## 四、目录规则

| 目录 | 上限 | 增长条件 |
|---|---|---|
| 根目录 | 3 | 冻结 |
| `.github/workflows/` | 1 | 冻结 |
| `docs/` | 3 + 计划数 | 三份主文档冻结；`docs/plans/` 每个开发任务一份 |
| `src/` | **1** | **永不新增** |
| `fixtures/` | ~15 | 每个游戏页面 1 个 |
| `tests/` | ~5 | 每个可测模块 1 个，与被测模块同名 |

`src/` 只允许一个文件。这条把架构规则 R6（单文件 ≤1200 行）
从约定变成物理事实：想拆文件规避行数限制，会先撞上目录规则。

`docs/plans/` 下的计划完成后**保留不删** —— 它是这次改动为什么这么做的历史证据。

## 五、改动是否需要批准

| 位置 | 架构层 | 需批准 |
|---|---|---|
| CFG 段 | 配置 | 否 |
| L4 段 | Tasks | 否 |
| L3 段 | Domain | 是 |
| L2 段 | Gateway | 是 |
| L1 段 | Core | 是 |
| L5 段 | Runtime | 是 |
| `fixtures/` `tests/` | 测试 | 否 |
| `docs/plans/` | 计划 | 否（但计划本身要过关卡一） |
| `.github/` | CI | 是 |

只有「加任务、调配置、补测试、写计划」免批准，其余一律要。

## 六、六条架构红线

详见 [架构设计](docs/02-架构设计.md)，此处只列可机械检查的判据：

- **R1** 新增任务只能改 `TASKS` 数组。diff 触及 L1–L3 或调度器即违规。
- **R2** L1–L3 禁止出现任何任务 id 字符串。用 id 列表搜索代码段，命中即违规。
- **R3** 单个 Task 的 `run` 不超过 40 行。
- **R4** L4、L5 禁止出现 `fetch`、`XMLHttpRequest`、`http.get`、`location.href =`。
- **R5** Gateway 公开函数冻结为 `read`、`act`、`actAll` 三个。
- **R6** 单文件不超过 1200 行（不含注释与 UI 模板字符串）。

## 七、禁止事项

脚本永远不得实现以下功能，即使被要求：

- 世界发言（含小喇叭、大喇叭）
- 任何消耗元宝的操作
- 装备强化
- 抢镖、团队押镖
- 出售装备、遗忘技能、解散血盟
- 自动使用紧急军令补充军令

## 八、工作环境

代码与文档**一律不落本地磁盘**。全部改动通过 GitHub 网页或 github.dev 完成，
测试与验证跑在 GitHub Actions 上。

原因：本地终端启用了透明加密，落盘文件会被加密，
且加密客户端未必把 git 加入白名单，推上去可能是密文。

## 九、真相源

`docs/` 下的 Markdown 是唯一真相源。
其他形式的文档（网页、外部知识库）只是快照，冲突时以仓库内容为准。
# 开发契约

本文件是硬约束，不是建议。违反其中任何一条的改动，一律退回重做。

## 一、六步流程

每个开发任务必须依次走完，**两道关卡由仓库所有者放行**：

1. **更新需求文档** —— 在 `docs/01-需求规格.md` 写清做什么、为什么、验收标准。只提交文档，不动代码。
2. **标注架构落点** —— 写明改动落在哪一层（CFG / L1–L5）、涉及哪些文件、是否需要批准。

> **关卡一 · 需求确认**：所有者放行后，才能修改 `src/`。

3. **实现** —— 只改第 2 步声明过的位置。若发现必须改动别处，**退回第 1 步**，不得就地扩大范围。
4. **Dry-run 验证** —— 开启 dry-run 跑一轮，提交日志中的 URL 序列供核对。此时零真实请求。

> **关卡二 · 执行确认**：所有者核对 dry-run 日志后放行，才能关掉 dry-run 实跑。

5. **实跑并记录** —— 执行一轮，对照预期，异常当场记录。
6. **更新变更日志** —— 在 `docs/03-变更日志.md` 追加一条。只追加，不改写历史。

## 二、目录规则

| 目录 | 上限 | 增长条件 |
|---|---|---|
| 根目录 | 3 | 冻结 |
| `docs/` | 3 | 新增须批准 |
| `src/` | **1** | **永不新增** |
| `fixtures/` | ~15 | 每个游戏页面 1 个 |
| `tests/` | 1 | 冻结，只增断言 |

`src/` 只允许一个文件。这条把架构规则 R6（单文件 ≤1200 行）
从约定变成物理事实：想拆文件规避行数限制，会先撞上目录规则。

## 三、改动是否需要批准

| 位置 | 架构层 | 需批准 |
|---|---|---|
| CFG 段 | 配置 | 否 |
| L4 段 | Tasks | 否 |
| L3 段 | Domain | 是 |
| L2 段 | Gateway | 是 |
| L1 段 | Core | 是 |
| L5 段 | Runtime | 是 |
| `fixtures/` `tests/` | 测试 | 否 |

只有「加任务、调配置、补测试」免批准，其余一律要。

## 四、六条架构红线

详见 [架构设计](docs/02-架构设计.md)，此处只列可机械检查的判据：

- **R1** 新增任务只能改 `TASKS` 数组。diff 触及 L1–L3 或调度器即违规。
- **R2** L1–L3 禁止出现任何任务 id 字符串。用 id 列表搜索代码段，命中即违规。
- **R3** 单个 Task 的 `run` 不超过 40 行。
- **R4** L4、L5 禁止出现 `fetch`、`XMLHttpRequest`、`http.get`、`location.href =`。
- **R5** Gateway 公开函数冻结为 `read`、`act`、`actAll` 三个。
- **R6** 单文件不超过 1200 行（不含注释与 UI 模板字符串）。

## 五、禁止事项

脚本永远不得实现以下功能，即使被要求：

- 世界发言（含小喇叭、大喇叭）
- 任何消耗元宝的操作
- 装备强化
- 抢镖、团队押镖
- 出售装备、遗忘技能、解散血盟
- 自动使用紧急军令补充军令

## 六、真相源

`docs/` 下的 Markdown 是唯一真相源。
其他形式的文档（网页、外部知识库）只是快照，冲突时以仓库内容为准。
